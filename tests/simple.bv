
parent('john) ~ 'jacob;
parent('jacob) ~ 'gdad;

grandfather(z) {
	relate x
	z ~ parent(parent(x)) 
};

ancestor(x) ~ x;
ancestor(x) {
	y ~ parent(x)
	relate ancestor(y)
};

head((x:xs)) ~ x;

tail((x:xs)) ~ xs;

second_elem((x:y:ys)) ~ y;

second_head(l) {
	relate head(tail(l))
};

# myfun(x) {
# 	relate x * 2 + 1
# };
