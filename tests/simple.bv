
parent('john) ~ 'jacob;
parent('jacob) ~ 'gdad;

grandfather(z) {
	y ~ parent(x)
	z ~ parent(y)
	relate x
};

head((x:xs)) ~ x;

tail((x:xs)) ~ xs;

second_elem((x:y:ys)) ~ y;

second_head(l) {
	relate head(tail(l))
};
